spring:
    cloud:
        config:
            server:
                bootstrap: false
            namespace: default
            enabled: true
            fail-fast: false 
            name: Cloud
            profile: dev, beanstalk, cloud, mysql
            label: master